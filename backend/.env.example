# Database URL
DATABASE_URL=postgresql+psycopg://localhost:5432/iscreami

# CORS origins — comma-separated list
CORS_ORIGINS=*

# Default serving size in grams
SERVING_SIZE_G=66.0

# Anthropic API key — required only for the enrich_ingredient MCP tool
ANTHROPIC_API_KEY=
